# Memory Index

- [Patching pinned git dependencies in this Flutter app](vendored-git-deps.md) — use a local `vendor/<pkg>` + `dependency_overrides` path override; don't hand-edit the pub cache.
- [conduit_vt colon-delimited SGR parsing](conduit-vt-colon-sgr.md) — its CSI parser silently dropped `:` sub-param separators, breaking fzf-style extended colors.
